export type PlaylistArrayType = {
  id: number;
  img_url: string;
  title: string;
  artist: string;
  albumName: string;
  time: string;
}[];

export type PlaylistMusicType = {
  id: number;
  img_url: string;
  title: string;
  artist: string;
  albumName: string;
  time: string;
};

export type MusicId = {
  currentMusicId: number;
};
